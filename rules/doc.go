// Package rules reads the ruling dataset behind the site's Rules tab — the
// tricky interactions collected from the community FAQ and from r/Riftbound
// threads — so that commands can build on the rulings rather than only serve
// them.
package rules
